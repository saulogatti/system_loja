# Apresentação

Telas (`@RoutePage`) e o roteador `auto_route`. BLoC/Cubit e widgets internos não entram nesta documentação gerada.

Navegação de páginas com `auto_route`; `Navigator` só para dialogs, bottom sheets e modais.
