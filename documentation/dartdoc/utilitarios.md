# Utilitários

`ResultStatus`, mapeamento de erros de repositório, constantes, exceções de validação e extensões.

A apresentação usa `when`/`switch` em `ResultStatus`; repositórios usam `try/catch` internamente.
