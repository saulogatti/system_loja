# Modelos de domínio

Entidades de negócio em `lib/core/models/`. Não acoplam tabelas Drift (`XxxRecord`) nem DTOs JSON.

Serialização de arquivo/importação fica em `lib/data/entry/` e `lib/data/models/`.
